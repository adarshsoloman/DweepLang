Just follow the below steps :

1. Install dependencies :  using `pip install -r requirements.txt` (or `uv sync`).
2. Download the models :   by running `python download.py` (one-time step).
3. Start the app using :   `run.bat` / `run.sh` or `python server/app.py`.
4. Open the browser UI :  at the URL shown in the terminal and enjoy fully offline English ↔ Hindi translation.
